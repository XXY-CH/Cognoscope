# UI 设计规范文档（v1.0）

> 面向 AI 辅助代码生成的完整前端设计规范。  
> 覆盖：设计令牌 · 布局骨架 · 全部页面交互 · 数据模型 · 无障碍 · 快捷键。  
> 文档格式支持：**PDF、EPUB**。视觉主题：**浅色 / 深色对等双主题**。  
> 分心检测数据源：**摄像头实时检测**。框架无关，不绑定任何具体组件库。

---

## 目录

1. [设计令牌（Design Tokens）](#1-设计令牌)
2. [全局布局骨架](#2-全局布局骨架)
3. [通用组件规范](#3-通用组件规范)
4. [A · 文件目录](#4-a-文件目录)
5. [B · 个人仪表盘](#5-b-个人仪表盘)
6. [C · 知识图谱](#6-c-知识图谱)
7. [D · 回收站](#7-d-回收站)
8. [E · 阅读界面](#8-e-阅读界面)
9. [数据模型速查](#9-数据模型速查)
10. [快捷键一览](#10-快捷键一览)
11. [无障碍规范](#11-无障碍规范)
12. [响应式断点](#12-响应式断点)
13. [已确认设计决策](#13-已确认设计决策)
14. [离线与本地优先](#14-离线与本地优先)

---

## 1 设计令牌

### 1.1 色板

所有颜色通过 CSS 自定义属性（Custom Properties）声明，切换主题只需替换 `:root[data-theme="dark"]` 覆盖值。

#### 语义色彩变量表

| 变量名 | 浅色值 | 深色值 | 用途 |
|---|---|---|---|
| `--bg-base` | `#FFFFFF` | `#1A1A1E` | 页面主背景 |
| `--bg-elevated` | `#F5F5F7` | `#242428` | 侧栏、卡片背景 |
| `--bg-hover` | `#EFEFF3` | `#2A2A30` | 悬停背景 |
| `--bg-active` | `#E6E9FB` | `#2E3350` | 选中行 / 选中导航项背景 |
| `--bg-surface` | `#FFFFFF` | `#2C2C32` | 浮层、模态框、下拉菜单 |
| `--border-subtle` | `#E0E0E5` | `#3A3A42` | 分隔线、卡片描边 |
| `--border-default` | `#C8C8D2` | `#4A4A54` | 输入框边框 |
| `--border-strong` | `#A8A8B6` | `#55555F` | 强调边框、拖拽手柄 |
| `--text-primary` | `#1A1A1E` | `#E8E8F0` | 正文 |
| `--text-secondary` | `#6B6B7A` | `#9090A0` | 次要说明 |
| `--text-tertiary` | `#8E8E9C` | `#75758A` | 时间戳、脚注、占位符 |
| `--text-disabled` | `#ADADBA` | `#55555F` | 禁用状态 |
| `--accent` | `#4A6CF7` | `#6B8CF9` | 主要强调色（按钮、链接、选中） |
| `--accent-hover` | `#3A5CE6` | `#5A7BE8` | 强调色悬停 |
| `--accent-subtle` | `#EEF1FE` | `#1E2A5A` | 强调色浅背景（标签、徽章、批量条） |
| `--danger` | `#E54545` | `#FF6B6B` | 删除、错误 |
| `--warning` | `#F5A623` | `#FFB830` | 警告 |
| `--success` | `#34C759` | `#4ADE80` | 成功、专注度高 |
| `--overlay` | `rgba(0,0,0,0.45)` | `rgba(0,0,0,0.65)` | 模态遮罩 |

对比度要求：`--text-primary` 对 `--bg-base` ≥ 7:1；`--text-secondary` / `--text-tertiary` 对背景 ≥ 4.5:1；`--accent` 上的白色文字 ≥ 4.5:1。

#### 数据可视化专用色（双主题下保持近似亮度）

| 变量名 | 浅色 | 深色 | 含义 |
|---|---|---|---|
| `--chart-focus` | `#4A6CF7` | `#6B8CF9` | 专注度曲线 |
| `--chart-fatigue` | `#F5A623` | `#FFB830` | 疲劳度曲线 |
| `--chart-distraction` | `#E54545` | `#FF6B6B` | 分心事件标记 |
| `--chart-reading` | `#34C759` | `#4ADE80` | 阅读时长 |
| `--chart-bg-grid` | `#E8E8EF` | `#2E2E38` | 图表网格线 |
| `--chart-axis` | `#9090A0` | `#6A6A78` | 轴线与刻度标签 |

#### 分心事件类型配色（用于图例、时间轴标记、徽章）

| 事件类型 | 语义 key | 浅色 | 深色 | 图标建议 |
|---|---|---|---|---|
| 喝水 | `drink` | `#22A7C2` | `#38C7E0` | 水杯 |
| 聊天 | `talk` | `#B565D8` | `#C98AE8` | 对话气泡 |
| 离开座位 | `away` | `#E54545` | `#FF6B6B` | 门/走出 |
| 看手机 | `phone` | `#F5761A` | `#FF9448` | 手机 |
| 打哈欠 | `yawn` | `#9A8C2E` | `#C4B441` | 张口脸 |
| 走神（视线离屏） | `gaze_off` | `#6B6B7A` | `#9090A0` | 眼睛斜视 |

### 1.2 字体

```
--font-sans: "Inter", "SF Pro Text", "PingFang SC", "Microsoft YaHei", system-ui, sans-serif;
--font-serif: "Source Serif 4", "Songti SC", Georgia, serif;   /* EPUB 正文可选 */
--font-mono: "JetBrains Mono", "SF Mono", Consolas, monospace;  /* 代码、数值 */
```

### 1.3 字号阶梯

| 变量 | 尺寸 / 行高 | 字重 | 用途 |
|---|---|---|---|
| `--text-display` | 32px / 40px | 700 | 仪表盘大数字 |
| `--text-h1` | 24px / 32px | 600 | 页面标题 |
| `--text-h2` | 18px / 26px | 600 | 卡片标题、分区标题 |
| `--text-h3` | 15px / 22px | 600 | 小节标题 |
| `--text-body` | 14px / 22px | 400 | 正文、表格单元 |
| `--text-sm` | 13px / 20px | 400 | 次要信息、表头 |
| `--text-xs` | 12px / 18px | 400 | 标签、时间戳、脚注 |
| `--text-reader` | 16px / 1.75（可调 14–24px） | 400 | 阅读器正文 |

### 1.4 间距 / 圆角 / 阴影 / 边框

```
间距（4px 基准）：--sp-1:4  --sp-2:8  --sp-3:12  --sp-4:16  --sp-5:20  --sp-6:24  --sp-8:32  --sp-10:40  --sp-12:48
圆角：--radius-sm:4  --radius-md:8  --radius-lg:12  --radius-xl:16  --radius-full:9999
边框：--border-width:1px
阴影（浅色）：
  --shadow-sm: 0 1px 2px rgba(16,16,24,.06)
  --shadow-md: 0 4px 12px rgba(16,16,24,.10)
  --shadow-lg: 0 12px 32px rgba(16,16,24,.14)
阴影（深色）：同结构，rgba(0,0,0,.36/.48/.60)，并叠加 1px 顶部内高光 rgba(255,255,255,.04)
```

### 1.5 动效

| 变量 | 值 | 用途 |
|---|---|---|
| `--dur-fast` | 120ms | hover、按钮按下、图标切换 |
| `--dur-base` | 200ms | 面板展开/收起、弹层进出 |
| `--dur-slow` | 320ms | 侧栏折叠、页面切换、图谱布局 |
| `--ease-standard` | `cubic-bezier(.2,0,0,1)` | 通用 |
| `--ease-out` | `cubic-bezier(0,0,.2,1)` | 元素进入 |
| `--ease-in` | `cubic-bezier(.4,0,1,1)` | 元素退出 |

规则：
- 一切颜色、背景、边框变化统一 `--dur-fast`。
- 布局尺寸变化（面板宽高）统一 `--dur-base` 或 `--dur-slow`，且必须使用 transform / width 过渡，禁止跳变。
- 尊重 `prefers-reduced-motion: reduce`：将所有时长降为 0ms，仅保留透明度变化。

### 1.6 层级（z-index）

| 层 | 值 | 内容 |
|---|---|---|
| base | 0 | 常规内容 |
| sticky | 10 | 表头、阅读器顶栏/底栏 |
| resizer | 20 | 面板拖拽手柄 |
| dropdown | 100 | 下拉菜单、右键菜单 |
| selection-toolbar | 200 | 划词浮动工具条 |
| drawer | 300 | 设置抽屉 |
| modal | 400 | 对话框、导入弹窗 |
| toast | 500 | 全局提示 |
| tooltip | 600 | 悬浮提示 |

### 1.7 图标与状态

- 图标统一线性风格，线宽 1.5px，尺寸 16 / 20 / 24 三档，颜色继承 `currentColor`。
- 纯图标按钮必须有 `aria-label` 与 tooltip（延迟 400ms 出现）。
- 交互态：默认 / hover（`--bg-hover`）/ active（`--bg-active`）/ focus-visible（2px `--accent` 外描边，offset 2px）/ disabled（opacity .4，`cursor: not-allowed`）。

---

## 2 全局布局骨架

所有 A–D 页面共用同一外壳（AppShell）；E 阅读界面为独立全屏布局，不含左侧全局导航栏。

```
┌──────────────┬───────────────────────────────────────────────┐
│  Sidebar     │  Main Region                                  │
│  (240px)     │  ┌─────────────────────────────────────────┐  │
│              │  │ PageHeader（页面标题 + 操作区）          │  │
│  ┌────────┐  │  ├─────────────────────────────────────────┤  │
│  │ 头像区 │  │  │                                         │  │
│  └────────┘  │  │ Content（可滚动）                        │  │
│              │  │                                         │  │
│  导航项 ×3   │  │                                         │  │
│              │  └─────────────────────────────────────────┘  │
│  ┌────────┐  │                                               │
│  │ 底部区 │  │                                               │
│  └────────┘  │                                               │
└──────────────┴───────────────────────────────────────────────┘
```

### 2.1 Sidebar（全局导航栏）

- 展开宽 `240px`，折叠宽 `64px`；折叠后仅显示图标并居中，文字通过 tooltip 提示。折叠状态持久化到本地存储。
- 背景 `--bg-elevated`，右侧 1px `--border-subtle` 分隔。
- 结构自上而下：

1. **用户区**（高 64px，padding `--sp-4`）
   - 左：圆形头像 32px（无头像时显示姓名首字母，背景取 `--accent` 的 12% 透明度）。
   - 中：用户名 `--text-body` 600，副行为账户类型 `--text-xs` `--text-tertiary`；折叠态隐藏。
   - 右：设置齿轮图标按钮 20px，点击打开设置抽屉（见 2.4）。
   - 整块 hover 显示 `--bg-hover`，点击头像打开用户菜单（个人资料 / 主题切换 / 退出登录）。

2. **主导航**（padding `--sp-2`，项间距 `--sp-1`）
   - 三项：文件目录（folder）、个人仪表盘（bar-chart）、知识图谱（share-nodes）。
   - 项规格：高 40px，圆角 `--radius-md`，左内边距 `--sp-3`，图标 20px 与文字间隔 `--sp-3`。
   - 选中态：背景 `--bg-active`，文字与图标 `--accent`，左侧 3px `--accent` 竖条（圆角右端）。
   - hover 态：背景 `--bg-hover`。

3. **弹性空隙**

4. **底部区**
   - 回收站入口（trash 图标，与主导航同规格，但视觉权重更低：文字用 `--text-secondary`）。
   - 折叠/展开切换按钮（chevron 图标，指向随状态翻转）。
   - 深浅主题切换按钮（sun/moon 图标；三态循环：跟随系统 → 浅色 → 深色，tooltip 说明当前态）。

### 2.2 PageHeader

- 高 56px，左右 padding `--sp-6`，`position: sticky; top: 0`，背景 `--bg-base` + 底部 1px `--border-subtle`。
- 左侧：页面标题（`--text-h1`，A/D 页可为面包屑）。
- 右侧：该页专属操作区。

### 2.3 Content

- 最大内容宽度不限（表格与图谱需要横向空间），左右 padding `--sp-6`，顶部 padding `--sp-5`。
- 纵向滚动条仅出现在 Content 内部，Sidebar 与 PageHeader 固定。

### 2.4 设置抽屉（Settings Drawer）

- 从右侧滑入，宽 `420px`，全高，`--shadow-lg`，遮罩 `rgba(0,0,0,.32)`。
- 左侧竖向分组 Tab：外观、阅读、检测与隐私、AI、快捷键、账户。
- 关键项：
  - 外观：主题（跟随系统/浅/深）、界面缩放（90%–120%）、正文字体族。
  - 阅读：默认字号、行距、页面模式（单页/双页/连续滚动）、EPUB 主题（纸白/夜间/护眼）。
  - 检测与隐私：摄像头开关、设备选择、预览开关、**"画面与推理特征完全在本地处理，不出设备"** 说明文字（常驻，不可折叠）、数据清除按钮。
  - AI：模型选择、回答语言、是否自动引用原文。
- 底部固定操作条：取消 / 保存（或即时生效并显示"已保存" toast）。

---

## 3 通用组件规范

| 组件 | 规格要点 |
|---|---|
| Button | 高 32（sm）/ 36（md）/ 40（lg）；主按钮实心 `--accent`，次按钮描边，文字按钮无边框；圆角 `--radius-md`；图标+文字间距 `--sp-2` |
| IconButton | 正方形 32×32，圆角 `--radius-md`，内含 20px 图标 |
| Input | 高 36，圆角 `--radius-md`，边框 `--border-default`，focus 时边框 `--accent` + 2px 同色 20% 外发光 |
| SearchInput | 左置 search 图标，右置清空按钮（有值时出现），支持 `Esc` 清空 |
| Table | 表头高 40，行高 52，行分隔 1px `--border-subtle`，行 hover `--bg-hover`，表头 sticky |
| Menu | 最小宽 180，项高 34，图标+文字，危险项文字 `--danger`，分组间 1px 分隔线 |
| Dialog | 宽 440（确认）/ 600（表单），圆角 `--radius-lg`，标题 `--text-h2`，底部右对齐按钮组 |
| Toast | 右下角堆叠，宽 320，自动消失 3s（错误型 6s 且带关闭按钮） |
| Tooltip | 深色气泡（深色主题下反相），`--text-xs`，最大宽 240 |
| EmptyState | 居中，插画/图标 64px + 主文案 `--text-h3` + 辅助说明 `--text-sm` + 主操作按钮 |
| Skeleton | 圆角矩形骨架，1.4s 循环微光动画；表格加载显示 6 行骨架 |
| ProgressRing | 用于专注度/疲劳度，尺寸 88，环宽 8，中心显示数值 |

---

## 4 A · 文件目录

### 4.1 布局结构

```
PageHeader
  左：「文件目录」面包屑 [当前文件夹名称]
  右：[搜索框] [刷新] [回收站] [新建文件夹] [导入]

Content
  [文件列表区 — 无限滚动或分页]
```

### 4.2 顶部操作栏

| 元素 | 规格 |
|---|---|
| 搜索框 | `SearchInput`，宽 220，placeholder="搜索文件名…"，支持模糊匹配与 `/` 快捷键聚焦 |
| 刷新按钮 | `IconButton` 20px refresh 图标；点击后图标旋转 600ms；tooltip "刷新目录" |
| 回收站按钮 | `IconButton` 20px trash 图标；tooltip "前往回收站"；导航到 D 页 |
| 新建文件夹 | `IconButton` 20px folder-plus 图标；tooltip "新建文件夹"；单击后在当前目录内联创建新文件夹并进入重命名模式 |
| 导入按钮 | 主要次级 Button（描边），图标 upload；点击打开导入 Dialog |

**导入 Dialog**（宽 600）：
- 拖拽区（高 160，圆角 `--radius-lg`，虚线边框，拖入文件后边框变 `--accent`）。
- 说明：支持 PDF、EPUB、Markdown、TXT，单次最多 20 个文件，单文件最大 200 MB。
- 正在导入时显示进度列表（文件名 + 进度条）。
- 可取消单个文件。
- 导入成功后 toast 提示"成功导入 N 个文件"。

### 4.3 文件列表（Table）

表头列：

| 列 | 宽度 | 说明 |
|---|---|---|
| 文件名 | 弹性（最小 240） | 文件类型图标（16px）+ 名称，可点击 |
| 更新时间 | 160 | 友好时间格式（"3 分钟前" / "昨天 14:32"），hover 展示操作按钮组 |
| 最后阅读时间 | 160 | 同上，hover 展示操作按钮组 |
| 文件大小 | 100 | 右对齐，`--text-sm` |

**行 hover 交互（核心规范）**：

当鼠标移入"更新时间"或"最后阅读时间"单元格时：
- 时间文字淡出（`opacity: 0`，`--dur-fast`），同时三个图标按钮淡入替换原文字位置，布局不发生偏移。
- 三个操作按钮依次为：
  1. 重命名（pencil 图标）：点击后时间列变为内联输入框，按 Enter 确认，Esc 取消；确认后 toast "已重命名"。
  2. 移动到（folder-arrow 图标）：点击打开文件夹选择器 Dialog（树形列表），选择目标文件夹后移动并 toast 提示。
  3. 删除（trash 图标，颜色 `--danger`）：点击弹出确认 Dialog，确认后移入回收站并 toast 提示（含撤销按钮，5s 内可撤销）。

**文件名列 hover 交互**：
- 鼠标移入整行时，文件名呈现 underline。
- 单击文件名 → 新标签页/当前页打开阅读界面（E）。
- 右键整行 → 右键菜单（打开、重命名、移动到、下载原始文件、删除）。

**文件夹行**：
- 文件类型图标为 folder-open（填充色 `--accent`）。
- 单击文件夹名 → 进入该文件夹（面包屑更新）。
- 面包屑：根目录 / 文件夹 A / 文件夹 B，点击任一级跳转。

**多选**：
- 勾选框默认隐藏，行 hover 或已选中时显示；点击行首勾选框选中该行。
- 表头勾选框三态：全不选 / 部分选（indeterminate）/ 全选。
- 多选时顶部出现批量操作条（吸附在 PageHeader 下方，高 48，背景 `--accent-subtle`）：
  - 左：「已选中 N 项」
  - 右：移动到、下载、删除

**空状态**：
- 当前文件夹无文件时：EmptyState 组件，图标为 empty-folder，主文案"暂无文件"，副文案"拖拽文件到此处，或点击导入"，操作按钮"导入文件"。
- 搜索无结果时：图标 search-empty，主文案"无匹配结果"，副文案"尝试修改关键词或检查是否在正确目录"，无操作按钮。

### 4.4 状态与筛选

- 列标题可点击排序（文件名 / 更新时间 / 大小），再次点击反向，第三次取消排序。
- 排序箭头图标：默认 `--text-disabled`，激活态 `--accent`。

---

## 5 B · 个人仪表盘

### 5.1 布局结构

```
PageHeader
  左：「个人仪表盘」
  右：[会话选择器 — 下拉，显示"最近 7 次 / 本月 / 全部"] [导出报告 PDF 按钮]

Content (滚动)
  ┌── 摄像头状态条 ──────────────────────────────────────┐
  ├── 指标卡行（4 列）────────────────────────────────────┤
  ├── 专注度 & 疲劳度折线图（全宽）──────────────────────┤
  ├── 分心事件时间轴（全宽）─────────────────────────────┤
  ├── 分心事件分类汇总（左 50%）| 阅读统计（右 50%）─────┤
  └── 阅读会话列表──────────────────────────────────────┘
```

### 5.2 摄像头状态条

- 高 40px，背景 `--bg-elevated`，圆角 `--radius-md`，padding `--sp-4`，margin-bottom `--sp-5`。
- 摄像头**开启**时：
  - 左侧绿色状态点（8px circle，`--success`，有呼吸动画 2s 循环）+ 文字「摄像头检测中」。
  - 右侧：摄像头小预览缩略图（60×45，可点击展开为 160×120 浮动画中画）+ 「关闭预览」按钮。
  - 最右：「暂停检测」按钮（文字按钮）。
- 摄像头**暂停**时：状态点变灰，文字「检测已暂停」，按钮改为「恢复检测」。
- 摄像头**未授权/不可用**时：状态点红色 + 「⚠ 摄像头不可用」+「前往设置」链接按钮。
- 点击整条状态条外的区域不应触发任何操作，避免误点。

### 5.3 指标卡（4 列）

每张卡规格：背景 `--bg-elevated`，圆角 `--radius-lg`，padding `--sp-5`，`--shadow-sm`。

| 卡片 | 主数值 | 副信息 | 图标颜色 |
|---|---|---|---|
| 专注时长 | hh:mm:ss | 「较上次 +N 分钟 ↑」或红箭头 | `--chart-focus` |
| 阅读行数 | N 行 | 「较上次 +N 行」，千分位分隔 | `--chart-reading` |
| 分心次数 | N 次 | 「最长一次 N 分钟」 | `--chart-distraction` |
| 疲劳预警 | N 次 | 「首次出现于 HH:MM」 | `--chart-fatigue` |

- 主数值用 `--text-display` 字号，卡片图标 32px 放右上角，颜色见上表。
- 底部可选迷你趋势条形图（高 32px，显示最近 7 次会话对比）。

### 5.4 专注度 & 疲劳度折线图

- 全宽，高 200px，左 padding 留给 Y 轴刻度（40px），底部留 24px 给时间轴。
- X 轴：会话内时间线（分钟）或日期（多次对比时）。
- Y 轴：0–100 百分比，网格线 `--chart-bg-grid`，5 条。
- 双折线：专注度（`--chart-focus`，实线）、疲劳度（`--chart-fatigue`，虚线 `4,3`）。
- 阈值参考线：专注度 ≥ 70 为绿色区间，疲劳度 ≥ 60 为橙色警告带（半透明填充）。
- 分心事件以三角形图标叠加在折线图对应时刻的 X 轴上，颜色按事件类型。
- hover 任意点：垂直参考线 + tooltip（时刻 / 专注度 / 疲劳度 / 当前事件）。
- 右上角图例可点击切换显示/隐藏各系列。

### 5.5 分心事件时间轴

- 全宽，高 64px，`--bg-elevated`，圆角 `--radius-md`。
- 内容：横向时间轴，事件用圆形徽章标记（颜色见 §1.1 事件配色），badge 内为事件图标。
- 聚集事件（同一秒内多个）合并为数字气泡，点击展开。
- hover 事件徽章 → tooltip：事件类型、时刻、持续时长。
- 右侧图例：所有出现过的事件类型，点击过滤。
- **手动修正**：右键（或长按）事件徽章弹出上下文菜单，包含两项：
  - 「标记为误判」：将该事件从统计中排除，徽章改为虚线描边半透明样式，tooltip 注明"已标记为误判"；可再次右键「取消误判标记」恢复。
  - 「修改类型」：展示事件类型选择器（6 种，带图标），选择后更新徽章颜色与图标，分类汇总实时刷新。
- 仪表盘分类汇总卡片中，被标记为误判的事件在"次数"列以删除线样式标注，不计入合计。

### 5.6 分心事件分类汇总（左半宽卡片）

- 竖向条形图 + 数据表。
- 每种事件类型一列（或一行），显示：次数 + 占比 + 合计时长。
- 右下角环形图（ProgressRing 变体，多环），外环→内环依次对应次数最多的前 3 类。
- 「查看详细记录」链接展开完整列表（可在当前卡片内展开或打开抽屉）。

### 5.7 阅读统计（右半宽卡片）

- 连续阅读天数：火焰图标 + 数字（大字）+ 日历热力图（最近 30 天，每格颜色深浅表示阅读分钟数）。
- 总阅读时间、总阅读行数同指标卡格式（行数带千分位分隔，如 12,480 行）。
- 最近阅读文件快速入口：最多 3 条，文件图标 + 文件名 + 最后阅读时间，点击跳转阅读界面。

### 5.8 阅读会话列表

- 表格：会话时间段 | 文件名 | 时长 | 专注度均值 | 分心次数 | 操作。
- 操作列：「查看详情」→ 展开行内详情（专注曲线缩略图 + 分心时间轴）。
- 按日期分组，分组行显示当日总时长。

---

## 6 C · 知识图谱

### 6.1 布局

```
PageHeader
  左：「知识图谱」
  右：[搜索节点] [布局切换：力导向 / 树形 / 时间线] [适应视图] [设置]

Content（全屏 Canvas 区域，无内边距）
  左侧可收起节点详情面板（280px）
```

### 6.2 画布交互

| 操作 | 行为 |
|---|---|
| 鼠标滚轮 | 缩放（10%–400%） |
| 拖拽空白区域 | 平移 |
| 点击节点 | 选中，左侧面板展示详情 |
| 双击节点 | 直接打开该文件（阅读界面） |
| 拖拽节点 | 手动调整位置，位置持久化 |
| 右键节点 | 打开、重命名、高亮关联节点、隐藏节点 |
| 右键画布空白 | 适应视图、重置布局 |

连线由 AI 自动生成，画布上**不提供**手动连线、删边、全选边等编辑操作。

### 6.3 节点规格

- 形状：圆形，直径 40px（文件）/ 32px（标签/关键词）/ 56px（文件夹）。
- 颜色：按文件类型（PDF `--chart-focus`、EPUB `--chart-reading`、MD `--chart-fatigue`）或按文件夹颜色（自定义）。
- 内部：文件类型图标 20px。
- 节点标签：文件名，最多两行，超出省略，`--text-xs`，位于节点下方。
- 选中态：3px `--accent` 光环 + `--shadow-md`。
- hover 态：节点放大 1.1x（`--dur-fast` transition）+ 周围关联连线高亮。

### 6.4 连线规格

- 颜色：默认 `--border-strong`（20% 透明度）；关联越强，线越粗（1–4px）、越不透明。
- 选中节点时：与之相连的边高亮（`--accent` 50%），非关联边变暗（10% 透明度）。
- 连线方式：曲线（力导向）/ 直线（树形）可切换。
- **知识图谱的边由 AI 在后台自动分析文档内容相似度后建立，UI 不提供手动建边、连线或删边操作**（仅保留查看与过滤）。AI 首次建边或重新分析时，画布顶部显示进度提示条（高 32px，`--bg-active`），完成后自动隐藏。

### 6.5 左侧节点详情面板

- 文件名（`--text-h2`）、类型徽章、文件大小、创建/最后阅读时间。
- 「打开阅读」主要按钮（全宽）。
- 「关联节点」列表：每项为缩略图标 + 文件名，点击跳转到画布上的该节点。
- 「批注摘要」：来自阅读界面的批注数量，最多展示 3 条最新批注预览，「查看全部」展开。
- 「标签」：可添加、删除自定义标签，标签可作为图谱分组依据。

### 6.6 搜索

- `SearchInput` 展开后实时过滤，匹配节点高亮，非匹配节点半透明（40% opacity）。
- 结果数 0 时画布提示"无匹配节点"但不清空图谱。
- Esc 清除搜索，恢复全部节点不透明度。

---

## 7 D · 回收站

与 A 文件目录共用同一 Table 组件，差异如下：

- PageHeader 标题：「回收站」，面包屑仅一级（不可进入子文件夹）。
- 操作栏：保留搜索，去掉"回收站入口"按钮，添加"清空回收站"按钮（次级危险样式，点击弹确认 Dialog）。
- 表头列：文件名 / 原始路径 / 删除时间 / 文件大小。
- 行 hover 操作按钮：还原（rotate-ccw 图标）、彻底删除（trash 图标，`--danger`）。
  - 还原：弹 Dialog 确认还原位置（若原路径已不存在则询问还原到根目录），完成后 toast 提示。
  - 彻底删除：弹确认 Dialog，确认后不可恢复，toast 提示。
- 无撤销机制（彻底删除）。
- 空状态：图标 trash-empty，主文案"回收站为空"，无操作按钮。
- 回收站内文件超过 30 天自动彻底删除，顶部展示提示条"自动清理说明：30 天后将自动永久删除"。

---

## 8 E · 阅读界面

### 8.1 整体布局

阅读界面为独立全屏路由（不含全局 Sidebar），通过 `文件目录/单击文件名` 进入。

```
┌───────────────────────────────────────────────────────────────┐
│  TopBar（高 48px，sticky）                                    │
├────────────┬──────────────────────────────┬──────────────────-┤
│  TocPanel  │  ReaderCanvas                │  SidePanel        │
│  (260px)   │  (弹性，min 480px)           │  (360px)          │
│  可收起    │                              │  可收起           │
│            │                              │  上：QA           │
│            │                              │  ↕ 可拖拽分隔条   │
│            │                              │  下：Annotations  │
├────────────┴──────────────────────────────┴──────────────────-┤
│  BottomBar（高 40px，sticky）                                  │
└───────────────────────────────────────────────────────────────┘
```

**宽度约束**：  
- TocPanel 默认 260px，可手动拖拽至 180–400px，折叠后 0px（`width: 0; overflow: hidden`，带过渡动画）。  
- SidePanel 默认 360px，可手动拖拽至 280–520px，折叠后 0px，同上。  
- ReaderCanvas 最小宽 480px；若 TocPanel + SidePanel 展开后导致 Canvas 小于 480px，优先收起 TocPanel。

### 8.2 TopBar

| 区域 | 元素 |
|---|---|
| 左 | ← 返回文件目录（`IconButton` + "文件目录" 文字），当前文件名（`--text-h3`，截断加省略号，tooltip 显示完整名） |
| 中 | 页面模式切换：单页 / 双页 / 连续滚动（SegmentedControl，仅 PDF 显示） |
| 右 | 字号增减（A- / A+），摄像头状态点（mini 版，hover 展 tooltip），目录显示/隐藏（icon），侧边栏显示/隐藏（icon） |

### 8.3 ReaderCanvas

#### PDF 渲染
- 使用 PDF.js（或等价库）渲染，每页独立 `<canvas>` 或 `<div>`。
- 分页模式：单页（居中显示，上下翻页）/ 双页（左右两列，宽屏时启用）/ 连续滚动（所有页垂直拼接，页间间距 `--sp-5`）。
- 背景：`--bg-base`（浅色），`#121216`（深色）；PDF 纸面背景为白色（浅色模式）/ `#2A2A2E`（深色模式可选护眼色，与设置联动）。
- 文字选中层（Text Layer）透明叠加，支持复制、搜索、划词。

#### EPUB 渲染
- 使用 epub.js（或等价库），内容注入样式变量，跟随全局主题。
- 支持字体族、字号、行距（设置中配置）。
- 分页或连续滚动（同 PDF，由设置控制）。

#### 缩放
- 底栏有缩放控件（50%–200%），也可 Ctrl+滚轮缩放。
- "适应页宽" / "适应页高" 快捷选项。

### 8.4 TocPanel（目录面板）

- 背景 `--bg-elevated`，右侧 1px `--border-subtle`。
- 顶部标签：「目录」/ 「书签」两 Tab。
- 目录树：缩进表示层级（每级 `--sp-4`），当前可视章节条目高亮（`--accent`），自动滚动至当前条目。
- 点击条目 → 跳转至对应页/锚点，过渡动画 `scroll-behavior: smooth`。
- 折叠按钮在 TopBar 右侧，点击后 TocPanel 宽度动画收至 0，按钮图标翻转。
- 拖拽手柄：TocPanel 右边缘，宽 4px，hover 变 `--accent`，`cursor: col-resize`，`z-index: resizer`。

### 8.5 BottomBar

- 背景 `--bg-base` + 顶部 1px `--border-subtle`，左右 padding `--sp-6`，高 40px。
- 左：当前页码输入框（可直接输入页码跳转）/ 总页数（`--text-sm`，`--text-tertiary`）。
- 中：进度滑块（0–100%，对应文档总进度，拖动跳页）。
- 右：已阅读行数（`--text-sm`，格式 "已读 1,248 行"，千分位分隔）+ 缩放控件（下拉或 -/+ 按钮）。
  - 行数统计规则：以渲染后的可视文本行为单位累计，同一行重复经过不重复计数；PDF 按文字层行分割，EPUB 按排版后行分割。字号或窗口宽度变化导致重排时，以当前排版重新折算，已读行数按文档字符进度等比映射，避免数值跳变。
- 翻页：← → 方向键或 PgUp/PgDn；单页模式底栏中央另有左右翻页箭头（`IconButton`，32×32）。

### 8.6 SidePanel（AI 问答 + 批注）

#### 面板整体
- 背景 `--bg-elevated`，左侧 1px `--border-subtle`。
- 顶部操作行（高 40px，padding `--sp-3`）：
  - 左：「整理习得」按钮（wand 图标，次级 Button）—— 见 8.9。
  - 右：折叠 SidePanel 按钮（`IconButton`）。

#### 上半区：AI 问答（QAPanel）

- **会话模型：每个文件对应一条独立对话历史，不跨文件、无会话分支。** SidePanel 内无会话切换器、无新建对话按钮；打开不同文件时自动加载该文件的对话记录。
- 默认高度为 SidePanel 高度的 50%，最小 120px，可被分隔条拖拽至最小。
- **消息流**：气泡式，用户消息右对齐（`--accent-subtle` 背景），AI 回答左对齐（`--bg-surface` 背景）。
- 每条 AI 回答右上角：复制、引用（将该回答引入批注）、重新生成，三个 `IconButton`。
- 若用户选中 PDF 文字后发起提问，消息气泡顶部显示引用块（`--border-subtle` 左侧竖线 + 引用文字 `--text-tertiary` 斜体，最多 3 行，超出折叠）。
- 底部输入行：多行文本框（最小 36px，最大 5 行自动扩展）+ 发送按钮（`--accent` 主按钮）。
- 输入框左侧：`@` 按钮（引用当前页/当前选中文字），点击展示菜单：「引用选中文字」「引用当前页」。
- 空状态：EmptyState，图标 chat-bubble，"向 AI 提问，或选中文字后点击「提问」"。

#### 拖拽分隔条

- 水平分隔条，高 6px（含视觉线 1px 居中），`cursor: row-resize`，hover/drag 时高亮 `--accent`，`z-index: resizer`。
- 拖拽时实时更新上下区域高度，松开后持久化到本地存储。
- 双击分隔条：快速切换 50:50 / 仅问答 / 仅批注 三种预设比例，循环切换。
- 隐藏规则：问答区高度 < 40px 视为收起；批注区高度 < 40px 视为收起；两区均收起时显示 EmptyState 提示两个展开按钮。

#### 下半区：批注（AnnotationPanel）

- 批注列表，默认按文档出现位置排序（页码 + 位置偏移）。
- 每条批注卡片（`--bg-surface`，`--radius-md`，`--shadow-sm`，padding `--sp-3`）：
  - 顶部：引用文字（高亮颜色对应批注颜色，最多 2 行，折叠）。
  - 批注正文（可编辑，支持 Markdown 基础语法）。
  - 底部：页码 + 时间戳，右侧 删除（`--danger`）/ 跳转到原文（arrow-square-out 图标）两按钮。
  - 点击卡片 → ReaderCanvas 跳转至对应位置并闪烁高亮 0.8s。
- 顶部工具行（批注区内，高 36px）：
  - 左：「全部 N」（数字徽章）。
  - 右：颜色过滤器（4 色圆形选择，可多选），排序（时间 / 位置）。
- 添加按钮：「+ 添加全文批注」（文字按钮），点击追加一条不带引用的空白批注卡片。

### 8.7 划词交互（选中文字浮动工具条）

选中 PDF/EPUB 中的文字后，在选区上方或下方（自动避开视口边缘）出现浮动工具条：

- 工具条高 36px，`--bg-surface`，`--shadow-md`，`--radius-md`，`z-index: selection-toolbar`。
- 按钮从左至右：高亮（highlight，含颜色选择子菜单：黄/绿/蓝/粉 4 色）、批注（pen）、提问（chat）、复制（copy）、词典（book）、搜索本文（search-within）。
- 点击「批注」→ SidePanel 展开（若收起），AnnotationPanel 顶部插入新批注卡片，引用块已填入选中文字，光标定位到批注正文输入框。
- 点击「提问」→ QAPanel 展开，输入框填入引用选中文字内容，光标定位到问题输入框后方。
- 点击「高亮」→ 立即高亮并关闭工具条；颜色子菜单 hover 时实时预览。
- 工具条出现时不影响滚动；点击工具条外任意区域取消选区并隐藏工具条。

### 8.8 批注高亮层

- 文字高亮以 SVG `<rect>` 或 CSS background-color 实现，opacity 0.3（选中态 0.5）。
- 四种颜色：黄 `#FFD740` / 绿 `#5BF070` / 蓝 `#47B5FF` / 粉 `#FF7AC6`（深色模式适当降低饱和度）。
- 鼠标 hover 高亮区域：显示批注预览 tooltip（批注内容前 60 字，有截断省略号）。
- 点击高亮区域：SidePanel AnnotationPanel 滚动至对应批注卡片并高亮展示（0.6s 背景色闪烁）。

### 8.9 整理习得（Digest）

- 点击 SidePanel 顶部「整理习得」按钮：
  1. 按钮进入 loading 状态（旋转图标，文字「整理中…」）。
  2. AI 根据当前文件的全部问答记录与批注，归纳用户习得内容，返回结构化 Markdown。
  3. 结果以全屏浮层 Dialog（宽 760，高 80vh，滚动内容区）展示，标题「阅读习得：{文件名}」。
  4. 浮层内容区：Markdown 渲染，支持复制全文、导出为 .md 文件。
   5. 关闭按钮在右上角，Esc 亦可关闭。

---

## 9 数据模型速查

供 AI 生成代码时对齐字段命名。仅描述 UI 渲染所需字段，不含后端持久化细节。

```ts
type FileType = 'pdf' | 'epub' | 'md' | 'txt' | 'folder';

interface FileNode {
  id: string;
  name: string;
  type: FileType;
  parentId: string | null;
  sizeBytes: number;          // folder 为 0
  updatedAt: string;          // ISO 8601
  lastReadAt: string | null;
  deletedAt: string | null;   // 非 null 即在回收站
  originalPath?: string;      // 回收站列表展示用
}

interface ReadingSession {
  id: string;
  fileId: string;
  startedAt: string;
  endedAt: string | null;
  durationSec: number;
  linesRead: number;              // 阅读行数（唯一计量单位，非物理距离）
  focusSamples: MetricSample[];   // 专注度时序
  fatigueSamples: MetricSample[]; // 疲劳度时序
  distractions: DistractionEvent[];
}

interface MetricSample {
  atSec: number;   // 相对会话起点的秒数
  value: number;   // 0-100
}

type DistractionKind = 'drink' | 'talk' | 'away' | 'phone' | 'yawn' | 'gaze_off';

interface DistractionEvent {
  id: string;
  kind: DistractionKind;          // 用户修改类型后即写入此字段
  atSec: number;
  durationSec: number;
  confidence: number;             // 0-1，低置信度可在 UI 上以虚线边框区分
  dismissed: boolean;             // 用户标记为误判，UI 置灰且不计入合计
  kindEditedByUser: boolean;      // 类型被手动修正过，徽章右下角加小铅笔角标
}

interface Annotation {
  id: string;
  fileId: string;
  page: number;             // EPUB 用 spine index
  anchor: string;           // CFI (EPUB) 或页内坐标序列化 (PDF)
  quotedText: string | null;
  body: string;             // Markdown
  color: 'yellow' | 'green' | 'blue' | 'pink';
  createdAt: string;
  updatedAt: string;
}

interface QaMessage {
  id: string;
  fileId: string;
  role: 'user' | 'assistant';
  content: string;
  quotedText: string | null;
  quotedPage: number | null;
  createdAt: string;
  status: 'pending' | 'streaming' | 'done' | 'error';
}

interface GraphNode {
  id: string;
  fileId: string | null;   // null 表示标签节点
  label: string;
  kind: 'file' | 'folder' | 'tag';
  fileType?: FileType;
  x: number | null;        // 手动拖拽后的持久化坐标
  y: number | null;
}

interface GraphEdge {
  source: string;
  target: string;
  weight: number;          // 0-1，映射连线粗细与不透明度
}

interface CameraState {
  status: 'active' | 'paused' | 'denied' | 'unavailable';
  deviceId: string | null;
  previewVisible: boolean;
}
```

### 加载与错误状态约定

每个数据区域都需实现四态，不得只做成功态：

| 状态 | 表现 |
|---|---|
| loading | Skeleton（表格 6 行 / 卡片占位 / 图表灰底） |
| empty | EmptyState 组件，文案见各章节 |
| error | 区域内居中错误块：警示图标 + "加载失败" + 具体原因 `--text-sm` + 「重试」按钮 |
| success | 正常渲染 |

流式输出（AI 回答）额外状态：`streaming` 时气泡末尾显示闪烁光标，「发送」按钮变为「停止生成」。

---

## 10 快捷键一览

### 全局

| 快捷键 | 行为 |
|---|---|
| `/` | 聚焦搜索框 |
| `Ctrl/Cmd + ,` | 打开设置抽屉 |
| `Ctrl/Cmd + B` | 折叠/展开 Sidebar |
| `Ctrl/Cmd + Shift + L` | 循环切换主题 |
| `Esc` | 关闭最上层浮层（tooltip → 菜单 → 抽屉 → 对话框依次） |
| `g` 然后 `f` / `d` / `g` / `t` | 跳转到 文件目录 / 仪表盘 / 知识图谱 / 回收站 |

### 文件目录 / 回收站

| 快捷键 | 行为 |
|---|---|
| `↑` `↓` | 移动行焦点 |
| `Enter` | 打开聚焦的文件 / 进入文件夹 |
| `F2` | 重命名聚焦项 |
| `Delete` | 删除聚焦项（回收站内为彻底删除，需确认） |
| `Ctrl/Cmd + A` | 全选 |
| `Backspace` | 返回上级文件夹 |

### 阅读界面

| 快捷键 | 行为 |
|---|---|
| `←` `→` / `PgUp` `PgDn` | 上一页 / 下一页 |
| `Home` / `End` | 首页 / 末页 |
| `Ctrl/Cmd + F` | 文档内搜索 |
| `Ctrl/Cmd + G` | 跳转到指定页 |
| `Ctrl/Cmd + 滚轮` / `Ctrl/Cmd + =` `-` | 缩放 |
| `Ctrl/Cmd + 0` | 缩放重置为适应页宽 |
| `t` | 切换目录面板 |
| `r` | 切换右侧面板 |
| `h` | 高亮选中文字（默认黄色） |
| `n` | 对选中文字新建批注 |
| `q` | 引用选中文字提问 |
| `Ctrl/Cmd + Enter` | 问答输入框发送 |

冲突规则：单字母快捷键仅在焦点不在输入框/文本域时生效。

---

## 11 无障碍规范

- **语义化结构**：Sidebar 用 `<nav>`，主内容 `<main>`，文件列表用 `<table>` 配 `<th scope="col">`，面板用 `role="region"` + `aria-label`。
- **键盘可达**：所有交互元素可 Tab 到达，顺序符合视觉顺序。拖拽分隔条需支持键盘：聚焦后方向键以 8px 步进调整，`Home`/`End` 到最小/最大。
- **焦点可见**：`:focus-visible` 统一 2px `--accent` 外描边 + 2px offset，禁止 `outline: none` 而无替代。
- **焦点管理**：Dialog/Drawer 打开时焦点移入首个可交互元素并锁定焦点在浮层内（focus trap），关闭后归还到触发元素。
- **hover 行为的键盘等价**：文件列表"时间列 hover 变操作按钮"的交互，键盘用户在行获得焦点时也需显示这些按钮；触屏设备通过行右侧「⋯」菜单提供同样操作。仅 hover 可达的功能不允许存在。
- **动态内容播报**：toast 用 `role="status"`（错误用 `role="alert"`），AI 流式回答容器用 `aria-live="polite"` 且完成后播报"回答完成"。
- **图表可访问**：每个图表提供 `aria-label` 概述，并附「查看数据表」切换，用表格呈现同样数据。
- **色彩不单独承载信息**：分心事件类型除颜色外必须带图标；专注/疲劳曲线除颜色外用实线/虚线区分。
- **对比度**：正文 ≥ 7:1，次要文字与大字 ≥ 4.5:1，图标与边框 ≥ 3:1。
- **减少动效**：遵循 `prefers-reduced-motion`，关闭位移与缩放动画，仅保留不超过 100ms 的透明度过渡。
- **摄像头隐私提示**：检测运行时必须有持续可见的视觉指示（状态点 + 文字），不允许静默采集。

---

## 12 响应式断点

| 断点 | 宽度 | 布局调整 |
|---|---|---|
| `xl` | ≥ 1440px | 全部面板默认展开；阅读界面 PDF 可用双页模式 |
| `lg` | 1200–1439px | 阅读界面默认收起 TocPanel；仪表盘指标卡 4 列 |
| `md` | 900–1199px | Sidebar 默认折叠为 64px；仪表盘指标卡 2 列；阅读界面 TocPanel 与 SidePanel 改为浮层覆盖（overlay）而非挤压布局 |
| `sm` | 600–899px | Sidebar 变为顶部抽屉；文件列表隐藏"文件大小"与"最后阅读时间"列；仪表盘单列；图表高度降至 160px |
| `xs` | < 600px | 文件列表改为卡片式（文件名 + 时间 + `⋯` 菜单）；阅读界面仅保留 Canvas，目录与侧栏均为全屏抽屉；知识图谱提示"建议在更大屏幕查看"但仍可用 |

规则：
- 布局采用 CSS Grid + `minmax()`，避免依赖 JS 计算宽度。
- 面板宽度用 `clamp()` 约束，超出断点时自动切换 overlay 模式。
- 触屏设备（`hover: none`）下所有 hover 触发的操作改为常显或收进 `⋯` 菜单。

---

## 13 已确认设计决策

以下为已拍板的产品边界，实现时以此为准，不得自行扩展。

| # | 议题 | 决策 | UI 影响 |
|---|---|---|---|
| 1 | 阅读距离定义 | **按行数计量**，单位为"行" | 底栏与仪表盘统一显示「已读 N 行」，不使用 km / m 等物理单位 |
| 2 | 算法权重暴露 | **不呈现**，不属于 UI 范畴 | 设置中无灵敏度权重滑块；仪表盘只展示 0–100 结果值 |
| 3 | 分心事件手动修正 | **允许** | 时间轴与分类汇总提供"标记为误判"与"修改类型"操作 |
| 4 | 知识图谱关联来源 | **AI 自动识别**，UI 不参与建边 | 无"生成关联"按钮、无手动连线交互；仅保留查看与过滤 |
| 5 | AI 问答会话模型 | **文件与对话一一对应**，无多轮分支 | SidePanel 无会话切换器、无新建会话按钮 |
| 6 | 批注协作 / 分享 | **不支持** | 批注卡片无作者头像、无权限标识、无回复层级 |
| 7 | 摄像头数据处理 | **完全本地处理**，画面与特征均不出设备 | 隐私文案明确写"完全本地"，无上传相关授权流程 |
| 8 | 离线可用 | **需要支持** | 增加离线状态指示与本地优先的数据状态提示（见 §14） |

---

## 14 离线与本地优先

应用默认可离线运行，所有文件、批注、检测数据均存储于本地。

### 14.1 网络状态指示

- 位置：Sidebar 底部，与主题切换按钮同排，宽度自适应。
- 在线态：不显示任何指示（避免噪音）。
- 离线态：显示胶囊标签「离线」（`--radius-full`，背景 `--bg-active`，文字 `--text-secondary`，前置 cloud-off 图标 16px），tooltip 说明「已离线，本地功能可正常使用」。
- 从离线恢复在线时：标签淡出，不弹 toast。

### 14.2 离线可用性矩阵

| 功能 | 离线可用 | 离线表现 |
|---|---|---|
| 文件目录浏览 / 重命名 / 移动 / 删除 | 是 | 完全一致 |
| 导入文件 | 是 | 完全一致 |
| PDF / EPUB 阅读、目录跳转、缩放 | 是 | 完全一致 |
| 划词高亮、批注增删改 | 是 | 完全一致 |
| 摄像头检测、专注/疲劳统计 | 是 | 完全一致（本地推理） |
| 仪表盘全部图表 | 是 | 完全一致 |
| 知识图谱查看、搜索、过滤 | 是 | 展示已有关联；不重新计算 |
| AI 问答 | 否（取决于模型部署位置） | 输入框禁用，placeholder 改为「离线状态下暂不可用」，发送按钮 disabled |
| 整理习得 | 否（同上） | 按钮 disabled，tooltip「需要连接 AI 服务」 |

若 AI 采用本地模型部署，上表最后两项改为"是"，此时不需要任何离线降级 UI。

### 14.3 无同步冲突 UI

因数据仅存本地、无多端同步与协作，**不设计**同步状态指示器、冲突合并界面、版本历史对比。若后续引入云同步，需另行补充规范。
