# AI-03 State Audit

- runId: `run_2026-08-03_ai03`
- objective: 在会话边界用本地批注、问答和 locator 生成可回看的结构化整理，并让问答按文件持久化。
- sourceOfTruth: `docs/GRAPH_ENGINEERING.md`, `docs/graph-engineering.json`, `.omx/specs/deep-interview-frontend-research-environment.md`, `.omx/plans/ai-era-research-environment-frontend.md`, `UI_spec.md`
- dependencies: `C2`, `C3`, `D2` 已满足；`KG-05` 仍因浏览器证据保持 `in_progress`，不阻塞本轮 AI-03。
- current evidence: `src/stores/qaStore.ts` 清空文件切换时的消息且未使用 IndexedDB；`runDigest` 只消费 annotations 并返回 Markdown；会话结束编排已存在但应继续保持 session/file stale guard。
- scope: QA message persistence, structured digest projection, AI/offline failure isolation, reader side panel states, additive IDB schema.
- non-goals: 外部检索、持续 AI popup、替换证据矩阵事实源、写作/TeX 能力、自动把模型输出标为 verified。
- stop conditions: 任何新 AI 请求都不阻塞 reader/annotation；旧文件响应不能写入新文件；无法结构化解析时保留 Markdown 草稿并显式标记。
