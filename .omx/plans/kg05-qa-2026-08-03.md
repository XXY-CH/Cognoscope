# KG-05 QA Evidence

## Automated Checks

- `npm run build` — passed (`tsc -b` and Vite production build).
- `npm run lint` — passed with three pre-existing warnings in
  `src/features/trash/TrashDialogs.tsx`, `src/features/file-directory/FileTable.tsx`,
  and `src/utils/pdfFrontMatter.ts`.
- `git diff --check` — passed.
- `node scripts/probe-evidence-matrix.mjs` — passed; verifies the v9-to-v10
  additive schema and route contracts.
- Node strip-types fixture probe — passed for PDF/EPUB locator gates, malformed
  and unresolved locators, missing sources, duplicate anchors, keyword file
  mapping, and the 20-anchor cap.
- Route shell curl checks — passed for `/`, `/knowledge-graph`,
  `/read/:fileId`, and `/evidence-matrix` on the local Vite server.

## Browser Gate

The browser runtime reported no available browser instances (`agent.browsers.list()`
returned `[]`). Screenshot, real PDF/EPUB interaction, viewport, keyboard, theme,
and reduced-preference checks therefore remain an environment gap, not a passing
claim. The implementation retains the required mobile dialog/focus-trap and
 reduced-motion/high-contrast contracts for the next browser-enabled run.
