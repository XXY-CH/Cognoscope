# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Monitor model files (large binaries, download separately)
monitor/models/*.task
monitor/models/*.pt
monitor/sessions/

# Codex-local Agent dependency graph overlay (keep out of the remote repo)
/docs/AGENT_DEPENDENCY_GRAPH.md
/docs/agent-dependency-graph.json

# Python bytecode / caches
__pycache__/
*.py[cod]
*$py.class
*.pyo
.Python

# graft's local graph cache — regenerable, not committed (run `graft build`).
graft/
.xuesen-runtime/
xuesen-main.zip

# Local Agent/editor integration state — machine-specific, not project runtime.
/AGENTS.md
.claude/
.cursor/
.ignore
.mcp.json
opencode.json
.omx/
